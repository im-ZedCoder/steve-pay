node_modules/
dist/
# The Cloudflare Pages output directory, assembled by scripts/build-pages.mjs. Deployed,
# never committed: it is a copy of the bundle plus a copy of public/.
dist-pages/
.wrangler/
.dev.vars
.env
.env.*
!.env.example
public/fonts/
public/assets/
coverage/
# Generated by scripts/cloudflare-setup.mjs. Holds the crypto secrets pushed to the
# Worker; losing API_KEY_PEPPER would invalidate every stored API key hash, so the file
# must be kept — but never committed.
.cloudflare.secrets.*.json
.cloudflare-secrets-*.tmp.json
*.log
.DS_Store
Thumbs.db